version: 2
jobs:
  build:
    docker:
      - image: circleci/golang:1.9
    working_directory: /go/src/github.com/jmdf/docker-registry-ls-go
    steps:
      - checkout
      - run: go build -o /tmp/docker-registry-ls